const elements = ['elem1', 'elem2', 'elem3'];
console.log(elements.join());

console.log(elements.join(' '))

console.log(elements.join(''))

console.log(elements.join('-'))

const cuvinte = ['this', 'is', 'a', 'test']
console.log(cuvinte.join(" "))